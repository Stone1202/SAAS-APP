import React from 'react';import { Typography, Card, Button } from 'antd';import { ArrowLeftOutlined } from '@ant-design/icons';import { useNavigate } from 'react-router-dom';const Page=()=>{const n=useNavigate();return(<div><div style={{marginBottom:16,display:'flex',alignItems:'center',gap:12}}><Button icon={<ArrowLeftOutlined/>} onClick={()=>n(-1)}>返回</Button><Typography.Title level={4} style={{margin:0}}>药房详情</Typography.Title></div><Card style={{borderRadius:10}}><Typography.Text type="secondary">药房详情 — 内容待补充</Typography.Text></Card></div>)};export default Page;